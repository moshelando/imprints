# Image Assets

The portfolio is self-contained.

- Mosaic Labs uses ten audited static PNG homepage previews in `/assets/work/`.
- Project sites use local SVG artwork and locally packaged assets.
- No random image-search hotlinks are required for the deployed portfolio.
- Webfonts, where specified by individual sites, are requested from their normal webfont providers with local/system fallbacks.
